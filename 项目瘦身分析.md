# 项目瘦身分析报告

## 1. 可以安全删除的目录（合计 ~350 MB）

### 1.1 AI训练检查点 (343 MB)
checkpoints/ 目录包含85个AI模型检查点文件，占用343MB空间。
建议：
- 保留最新的1-2个checkpoint（formal_v1, modular_v4）
- 删除实验性和过时的checkpoint（exp1, exp2, profile_*, speed_*等）
- 可节省约 300+ MB

### 1.2 构建产物 (1.76 MB)
build/ 目录包含Cython编译的中间文件
建议：完全删除，可通过重新编译恢复

### 1.3 重复的技能配置
.claude/ 和 .agents/ 目录存在大量重复的skill配置
建议：统一到 .agents/skills/ 目录，删除 .claude/skills/

### 1.4 Claude worktrees
.claude/worktrees/ 包含临时工作树副本
建议：删除未使用的worktrees

## 2. 代码优化建议

### 2.1 后端代码 (backend/)
当前：180个文件，14.4 MB

潜在优化：
- backend/engine/ai/log/ 目录包含多个实验日志目录
  建议：仅保留最新的正式版本日志
  
- backend/tests/ 和 tests/ 存在重复
  建议：统一测试代码到 tests/ 目录

- backend/sim/traits/ 特性系统
  需要检查是否有未使用的特性定义

### 2.2 数据文件 (data/)
当前：2356个文件，4.58 MB

可能的冗余：
- data/sprites/ 精灵图片
- data/redirect/ 重定向配置
  建议：检查是否有重复或未使用的条目

### 2.3 Wiki数据 (wiki/)
当前：1497个文件，7.93 MB

建议：
- 检查是否有重复的精灵/技能条目
- 清理meta/目录中的旧版本fold

### 2.4 前端优化
frontend/public/sprites/ 和 _attachments/sprites/ 可能存在重复图片
frontend/dist/ 可以删除（构建产物）

## 3. 立即可执行的清理操作

### 命令1：删除构建产物
Remove-Item -Recurse -Force build/

### 命令2：删除旧检查点（保留最新2个）
保留：checkpoints/formal_v1, checkpoints/modular_v4
删除其他

### 命令3：删除重复的skill配置
Remove-Item -Recurse -Force .claude/skills/

### 命令4：清理前端dist
Remove-Item -Recurse -Force frontend/dist/ -ErrorAction SilentlyContinue

## 4. 预计节省空间

- 构建产物: ~2 MB
- 旧检查点: ~300 MB  
- 重复skills: ~5 MB
- Claude worktrees: ~5 MB
- 其他优化: ~10 MB

**总计可节省: 约 320+ MB**

## 5. 需要人工review的部分

1. backend/engine/ai/log/ 下哪些实验日志需要保留
2. checkpoints/ 中哪些checkpoint是重要的
3. data/sprites/ 是否有未使用的精灵图片
4. wiki/ 中是否有过时的条目

