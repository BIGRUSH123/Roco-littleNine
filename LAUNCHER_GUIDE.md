# 🚀 训练启动器使用指南

## 快速开始

### 1. 安装依赖

```bash
pip install streamlit
```

### 2. 启动训练启动器

```bash
streamlit run training_launcher.py
```

浏览器会自动打开 `http://localhost:8501`

---

## 界面说明

### ⚙️ 基本参数（第一个标签页）

**训练规模：**
- **每轮对局数**：每轮迭代进行的自我博弈对局数（推荐：200）
- **迭代轮数**：训练的总轮数（推荐：10）
- **每轮训练 Epochs**：每轮神经网络训练的 epoch 数（推荐：20）

**MCTS 配置：**
- **模拟次数**：每步 MCTS 搜索的模拟次数
  - 串行：200-400
  - 并行：800-1600（推荐）
- **启用 MCTS 并行化** ✅：强烈推荐！2x 加速
- **并行 Worker 数**：推荐设置为 CPU 核心数（通常 4-8）
- **采样温度**：1.0 为标准随机性

**模型配置：**
- **批次大小**：256（推荐）
- **学习率**：0.001（推荐）

**设备配置：**
- 自动检测：自动选择 GPU 或 CPU
- CUDA：强制使用 GPU
- CPU：强制使用 CPU

**输出配置：**
- **运行名称**：为本次训练命名（例如：experiment1）
- **模型输出路径**：保存训练好的模型

---

### 🔧 高级参数（第二个标签页）

**训练策略：**
- **经验回放缓冲**：保留最近 N 轮数据（默认：5）
- **权重衰减**：L2 正则化系数（默认：0.0001）
- **Dropout 比率**：防止过拟合（默认：0.0）
- **隐藏层配置**：神经网络结构（默认：256,128）

**MCTS 高级选项：**
- **根节点噪声**：增加探索性（默认：0.25）
- **叶节点批量大小**：批量评估叶节点（默认：16）
- **最大回合数**：单局回合上限（默认：200）
- **平局判定阈值**：分差小于此值判平局（默认：0.02）

**评估和门控：**
- **评估对局数**：每轮评估新模型的对局数（默认：20，0=关闭门控）
- **晋升阈值**：胜率≥此值才替换最优模型（默认：0.55）

**并行和性能：**
- **自我博弈并行数**：多进程同时运行对局（默认：1）
- **进度输出频率**：每 N 局输出一次进度（默认：10）

---

### 🚀 启动训练（第三个标签页）

**功能：**

1. **命令预览**：显示生成的完整训练命令
2. **配置摘要**：显示关键参数的汇总
3. **启动按钮**：点击后启动训练

**预计指标：**
- 总对局数
- 总 Epochs
- 预计训练时间
- 预计加速比

**启动后：**
- 侧边栏显示「训练运行中」
- 可以点击「停止训练」按钮终止

---

### 📊 监控（第四个标签页）

- 显示训练状态
- 实时日志（开发中）
- 关键指标（模拟数据）

**建议**：使用专门的监控工具获得更好体验
```bash
python training_monitor.py
```

---

## 推荐配置

### 配置 1：快速测试（10 分钟）

**基本参数：**
- 每轮对局数：10
- 迭代轮数：1
- 每轮训练 Epochs：5
- 模拟次数：400
- ✅ 启用 MCTS 并行化
- 并行 Worker 数：4

**用途**：快速验证配置是否正确

---

### 配置 2：标准训练（10 小时）

**基本参数：**
- 每轮对局数：200
- 迭代轮数：10
- 每轮训练 Epochs：20
- 模拟次数：800
- ✅ 启用 MCTS 并行化
- 并行 Worker 数：4

**高级参数**：使用默认值

**用途**：生产环境标准训练

---

### 配置 3：深度训练（30 小时）

**基本参数：**
- 每轮对局数：300
- 迭代轮数：20
- 每轮训练 Epochs：30
- 模拟次数：1600
- ✅ 启用 MCTS 并行化
- 并行 Worker 数：8

**高级参数：**
- 经验回放缓冲：10
- 隐藏层配置：512,256,128

**用途**：追求最佳性能

---

## 使用流程

### 步骤 1：配置基本参数

1. 在「基本参数」标签页设置：
   - 训练规模（对局数、迭代轮数）
   - MCTS 配置（**务必启用并行化**）
   - 模型和设备配置
   - 运行名称（例如：exp1）

### 步骤 2：调整高级参数（可选）

1. 在「高级参数」标签页：
   - 大多数情况使用默认值即可
   - 如需精细调整，参考上面的说明

### 步骤 3：启动训练

1. 切换到「启动训练」标签页
2. 查看命令预览和配置摘要
3. 确认无误后点击「🚀 启动训练」
4. 等待训练开始（侧边栏显示状态）

### 步骤 4：监控进度

**方法 1：在启动器中监控**
- 切换到「监控」标签页
- 查看基本状态（简化版）

**方法 2：使用专门监控工具（推荐）**
```bash
# 打开新终端
python training_monitor.py --log-dir backend/engine/ai/log/exp1
```

### 步骤 5：停止训练（如需）

- 在侧边栏点击「⏹️ 停止训练」

---

## 常见问题

### Q1：参数太多，不知道怎么设置？

**A：使用推荐配置**
- 快速测试：对局 10，迭代 1
- 标准训练：对局 200，迭代 10
- 其他参数保持默认

### Q2：必须启用 MCTS 并行化吗？

**A：强烈推荐！**
- 启用后：2x 加速，10h 完成
- 不启用：原速，20h 完成

### Q3：Worker 数设置多少？

**A：推荐 4**
- 4 核 CPU：4
- 8 核 CPU：4-8
- 16 核 CPU：8

### Q4：启动后可以关闭浏览器吗？

**A：不建议**
- 关闭浏览器会终止训练
- 如需后台运行，使用命令行：
  ```bash
  python -m backend.engine.ai.train --battles 200 --sims 800 --mcts-parallel &
  ```

### Q5：如何查看详细日志？

**A：使用监控工具**
```bash
python training_monitor.py
```

### Q6：训练中途可以修改参数吗？

**A：不可以**
- 需要停止当前训练
- 重新配置参数
- 启动新训练

---

## 性能对比

### 串行 vs 并行

| 配置 | 模拟次数 | 单轮时间 | 10 轮总时间 |
|------|---------|---------|------------|
| 串行 | 200 | 2.0h | 20h |
| 串行 | 800 | 8.0h | 80h |
| **并行（推荐）** | **800** | **4.0h** | **40h** |

### 总加速比

| 阶段 | 加速 | 累计 |
|------|------|------|
| Python 优化 | 2.48x | 2.48x |
| Cython | 1.09x | 2.70x |
| **并行化** | **1.97x** | **5.31x** |

**启用并行后：800 次模拟 = 串行 400 次模拟的时间！**

---

## 快速命令参考

```bash
# 启动训练启动器
streamlit run training_launcher.py

# 启动监控工具
python training_monitor.py

# 命令行启动训练（等效于 UI 配置）
python -m backend.engine.ai.train \
    --battles 200 \
    --sims 800 \
    --mcts-parallel \
    --mcts-workers 4 \
    --iterations 10
```

---

## 立即开始

```bash
streamlit run training_launcher.py
```

**3 步完成配置，开始训练！** 🚀
