# 特性系统重构计划

## 目标

将特性系统从"每个特性一个 Python 类"重构为三层架构，与技能系统的数据驱动范式对齐。

## 三层架构

```
Layer 1: JSON 数据定义 (141 个特性)
  → 触发时机 + 条件 + 效果列表
Layer 2: 通用引擎 (trait_engine.py)
  → 解析 JSON → DataDrivenTrait 实现 TraitHandler 接口
Layer 3a: 完整 Python 类 (9 个特性)
  → 多 hook + 内部状态机
Layer 3b: Hook 注册回调 (9 个特性)
  → register_hook() 纯函数，无内部状态
```

---

## 18 个引擎更新方向

| # | 方向 | 状态 |
|---|------|------|
| 1 | team_counter 读写 (pre-entry accumulator) | ✅ |
| 2 | Aura 光环 (auto entry+leave 配对) | ✅ |
| 3 | 条件性 replace (conditional_replace) | ✅ |
| 4 | 效果变异 (mutate_effect) | ✅ |
| 5 | ref 表达式动态 heal/damage | ✅ |
| 6 | 印记操作 (dispel/steal/convert mark) | ✅ |
| 7 | 队伍级计算路径 | ✅ |
| 8 | 技能能耗/底材过滤 | ✅ |
| 9 | Sprite counters 读写 | ✅ |
| 10 | bloodline 检查 | ✅ |
| 11 | 效果继承 (inherit_effects) | ✅ |
| 12 | 技能计数 (skills[element=X].count) | ✅ |
| 13 | Hook 注册机制 (register_hook) | ✅ |
| 14 | Layer 3a traits | ✅ |
| 15 | Stub traits | ✅ |
| 16 | opponent.team_counters 路径解析 | ✅ |
| 17 | _simple.py 全量迁移 | ✅ |
| 18 | 批量 JSON 创建 | ✅ |

---

## 设计决策

### 决策 1: 条件 DSL

**条件来源表:**

| 来源路径 | 示例字段 | 操作 |
|---------|---------|------|
| `use.skill` | energy_cost, element, name, is_attack, is_defense, is_status | eq, neq, gt, gte, lt, lte, in, not_in |
| `use` | is_first, is_countered | bool |
| `use.modifiers` | power_mult, damage_mult, multi_hit | gt, lt, eq |
| `self` | energy, hp_pct, species.elements, first_action, _charging, has_countered_this_turn | eq, gt, lt, in, not_in, bool |
| `target` | energy, bloodline, hp_pct | eq, gt, lt, in, not_in |
| `battle` | globals.weather | eq, in |
| `player` / `opponent` | lives, team 构成 | eq, gt |

**Effects 条件路径语法:**
```
effects[<filter>].<property>
  filter: name=X | type=abnormal | type=stat | steps>0 | steps<0
  property: exists | steps | stacks | count
```

**新增路径:**
```
self.counters[<key>]                    → sprite counter 值
self.skills[element=X].count            → 匹配技能数
team_counters[<key>]                    → 己方队伍计数器
opponent.team_counters[<key>]           → 对方队伍计数器
player.team_counters[<key>]             → 玩家队伍计数器
player_fainted_count / opponent_lives   → 预计算聚合值
```

**组合:** `{"and": [...], "or": [...], "not": {...}}`

### 决策 2: Modifier Schema

**三层写入目标:**

| 目标 | 生命周期 | 示例字段 |
|------|---------|---------|
| `use_modifiers` | 单次使用 | power_mult, damage_mult, damage_reduction, multi_hit, ignore_mods, priority_mod |
| `battleskill_mut` | 跨回合持久 | power_mod, sealed, cooldown, priority, _transmission, energy_cost, priority_mod_perm |
| `battleskill_temp` | 单次使用（非 modifiers dict） | priority_mod_temp（引擎内部映射） |

**battleskill_mut filter 扩展:** energy_cost_lt, energy_cost_gt, energy_cost_gte, energy_cost_eq, is_defense, is_status

**op 语义:**

| 字段 | 初始值 | add | mult | set |
|------|--------|-----|------|-----|
| power_mult | 1.0 | current += value | current *= value | = value |
| damage_mult | 1.0 | current += value | current *= value | = value |
| damage_reduction | 0.0 | current += value | — | = value |
| multi_hit | 0 | current += value | — | = value |
| ignore_mods | false | — | — | = value |

### 决策 3: 跨触发器状态

**JSON 不需要 state 字段。** 状态存储在:

| 存储位置 | JSON 能力 | 实现方式 |
|---------|----------|---------|
| sprite.effects | 读/写/替换 | mode: "replace" / "conditional_replace" + source 匹配 |
| sprite.counters / flags | 读/写 | flags 效果类型 + counters.xxx 路径 |
| battle.team_counters / pending_effects | 读/写 | accumulator 条件 + pending_effects 触发 |

**引擎原语:**
- `ref 表达式`: `"steps": "=@self.energy"` 动态求值
- `mode: "replace"`: 同 source 旧效果先清除再添加
- `mode: "conditional_replace"`: clear_condition 满足时清除+添加
- `flags 效果`: `{"flags": {"counters.xxx": 0, "_escape_pending": true}}`
- `team_counters 写入`: `{"team_counters": {"key": delta}}`

### 决策 4: Layer 3 名单

**3a: 完整 TraitHandler 子类 (9)**
- 不朽: 力竭后复活状态机
- 宝剑王牌 / 正位宝剑: 槽位封印
- 嫁祸: HP 四分位状态机
- 对流 / 倾轧: 自定义能量计算
- 腾挪 / 保卫 / 好象坏象: 变身为棋绮后

**3b: Hook 注册回调 / 特殊逻辑 (9)**
- 吟游之弦: before_apply_mark hook
- 守望星: before_consume_starfall hook
- 多人宿舍: max_energy_override hook
- 机械变式: after_transmission hook
- 星地善良: turn_end_bench_check hook
- 系统发育: after_gain_energy hook
- 刺肤: after_take_damage hook
- 石头大餐: on_energy_short hook
- 张弛有度: datetime 判断

**非 list[str] 返回类型的 hook 禁止数据驱动:**
- on_energy_short (→ int): 全归 Layer 3b
- on_fatal_damage (→ bool): 惊吓、逐魂鸟保留 Python

**Stub 特性 (4，未实现):**
- 契约的形状, 稀兽花宝, 嫉妒, 无忧无虑

---

## 迁移路径

### 阶段 1: 基础设施 ✅
- [x] 修复嫁祸 _THRESHOLDS 类级 dict bug → `sprite.counters['scapegoat_quarters']`
- [x] 新建 trait_engine.py: DataDrivenTrait + ConditionEvaluator + RefResolver + Hook 注册机制
- [x] 用 专注力/养分内循环/生长 做概念验证 (3/3 通过)

### 阶段 2: 批量迁移 ✅
- [x] _simple.py 全部迁移 (12/12)
- [x] _conditional.py 条件特性迁移 (50/53 → 仅剩 Layer 3a + 惊吓/逐魂鸟)
- [x] _complex.py 可数据化特性迁移 (79/~100 → 仅剩 Layer 3a/3b/stub)
- [x] 141 个 JSON 特性文件创建完成

### 阶段 3: 引擎增强 ✅
- [x] Hook 注册机制（register_hook / unregister_hook / fire_hook / fire_hook_first）
- [x] Aura 自动配对 (entry + leave 触发器)
- [x] Conditional replace (clear_condition + effects_mode)
- [x] Mutate effect (效果变异: delta_steps / delta_stacks)
- [x] ref 表达式动态值 (heal/damage/stat steps)
- [x] 印记操作 (dispel_mark / steal_mark / convert_mark)
- [x] 效果继承 (inherit_effects)
- [x] 队伍计数器 (opponent.team_counters / player.team_counters)
- [x] battleskill_mut filter 扩展 (energy_cost_lt/gt/gte/eq, is_defense, is_status)
- [x] opponent.team_counters 路径解析

### 阶段 4: 收尾
- [x] 删除已迁移的旧 Python 类 (129 个类移除)
- [x] 24 个保留 Python 特性确认
- [ ] 全量回归测试

---

## 进度

**当前:** 阶段 4 收尾中 — 141 个 JSON 特性加载验证通过，24 个 Python 特性保留。

**最后更新:** 2026-05-11

### 数据统计

| 指标 | 数量 |
|------|------|
| JSON 数据特性 | 141 |
| Python Layer 3a | 9 |
| Python Layer 3b | 9 |
| Python Stub | 4 |
| Python 特殊 (fatal_damage) | 2 |
| **总计** | **165** |
| 移除的 Python 类 | 129 |

### 新增文件
- `scripts/sim/traits/trait_engine.py` — 通用特性引擎 (DataDrivenTrait + ConditionEvaluator + RefResolver + Hook 系统)
- `data/traits/` — 141 个 JSON 特性定义文件

### 修改文件
- `scripts/sim/traits/__init__.py` — get_trait() 数据优先
- `scripts/sim/__init__.py` — 加载 data/traits/
- `scripts/sim/traits/_simple.py` — 全部移除 (12/12)
- `scripts/sim/traits/_conditional.py` — 移除 50 个类，保留 3 个
- `scripts/sim/traits/_complex.py` — 移除 79 个类，保留 21 个
- `架构修改.md` — 本文件
