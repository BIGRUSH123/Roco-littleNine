# 项目代码瘦身完整方案

## 执行摘要

项目当前占用约 **170 MB**（不含env和node_modules）
通过以下优化可减少 **320+ MB** 空间

## 一、立即可执行的安全清理（无风险）

### 1.1 删除构建产物（2 MB）
```powershell
Remove-Item -Recurse -Force build/ -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force frontend/dist/ -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force frontend/.vite/ -ErrorAction SilentlyContinue
```

### 1.2 删除重复的skill配置（5 MB）
```powershell
# .claude/skills 是 .agents/skills 的完整副本
Remove-Item -Recurse -Force .claude/skills/ -ErrorAction SilentlyContinue
```

### 1.3 删除临时worktrees（5 MB）
```powershell
Remove-Item -Recurse -Force .claude/worktrees/ -ErrorAction SilentlyContinue
```

### 1.4 删除旧实验日志（~20 MB）
```powershell
# 保留 formal_v1，删除其他实验日志
Remove-Item -Recurse -Force backend/engine/ai/log/exp1/
Remove-Item -Recurse -Force backend/engine/ai/log/exp2/
Remove-Item -Recurse -Force backend/engine/ai/log/profile_*/
Remove-Item -Recurse -Force backend/engine/ai/log/speed_*/
Remove-Item -Recurse -Force backend/engine/ai/log/gpu_*/
```

**小计：约 32 MB**

## 二、需要review的清理（中等风险）

### 2.1 AI训练检查点优化（300 MB）

当前checkpoints/目录有85个文件，343 MB

**建议保留：**
- `formal_v1` - 正式训练版本1
- `modular_v4` - 模块化版本4（最新）
- `mcts_v2_formal` - MCTS正式版本2
- `validation_leaf16_20260608` - 验证版本

**建议删除：**
- `exp1`, `exp2` - 早期实验
- `profile_*` - 性能分析用的临时checkpoint
- `speed_*` - 速度测试用的临时checkpoint
- `gpu_batch_queue_smoke` - 冒烟测试
- `mcts_fix_scratch`, `mcts_fix_v1` - 已被v2替代
- `modular_v1`, `modular_v2`, `modular_v3` - 已被v4替代

```powershell
# 仅作参考，请先确认
Remove-Item -Recurse -Force checkpoints/exp1/
Remove-Item -Recurse -Force checkpoints/exp2/
Remove-Item -Recurse -Force checkpoints/profile_*/
Remove-Item -Recurse -Force checkpoints/speed_*/
Remove-Item -Recurse -Force checkpoints/gpu_*/
Remove-Item -Recurse -Force checkpoints/mcts_fix_*/
Remove-Item -Recurse -Force checkpoints/modular_v[123]/
```

**可节省：约 300 MB**

### 2.2 重复的精灵图片

检测到3个位置存储精灵图片：
- `frontend/public/sprites/`
- `frontend/dist/sprites/`（构建产物）
- `_attachments/sprites/`

**建议：**
```powershell
# dist已在1.1删除
# 如果_attachments是备份，可以删除
# Remove-Item -Recurse -Force _attachments/sprites/
```

## 三、代码结构优化建议（需要重构）

### 3.1 超大文件拆分

以下文件行数过多，建议拆分：

1. **backend/common/skill_trait_ids.py** (1997行)
   - 纯数据文件，考虑改为JSON配置

2. **backend/sim/battle.py** (1978行)
   - 建议拆分为多个模块：
     - `battle_core.py` - 核心战斗逻辑
     - `battle_effects.py` - 效果处理
     - `battle_state.py` - 状态管理

3. **backend/engine/replayer.py** (1692行)
   - 可拆分为：
     - `replay_parser.py` - 解析逻辑
     - `replay_executor.py` - 执行逻辑

4. **backend/engine/ai/train.py** (1669行)
   - 可拆分为：
     - `train_loop.py` - 训练循环
     - `train_data.py` - 数据处理
     - `train_eval.py` - 评估逻辑

### 3.2 过多导入的文件优化

以下文件导入过多（>20个），可能存在职责过重：

1. **backend/vm/executor.py** (79个导入)
2. **backend/engine/replayer.py** (74个导入)
3. **backend/vm/compiler/passes/skill_parse.py** (51个导入)

建议：将这些文件拆分成更小的模块，每个模块职责单一

### 3.3 测试代码整合

当前存在两个测试目录：
- `backend/tests/`
- `tests/`

建议：统一到 `tests/` 目录，删除 `backend/tests/`

## 四、使用自动化脚本

已创建两个脚本：

1. **scripts/code_cleanup.py** - 自动执行安全清理
   ```bash
   python scripts/code_cleanup.py
   ```

2. **scripts/cleanup_project.py** - 清理缓存文件
   ```bash
   python scripts/cleanup_project.py
   ```

## 五、长期优化建议

### 5.1 数据文件优化
- `data/` 目录2356个文件，检查是否有重复或未使用的条目
- 考虑将JSON数据压缩或合并

### 5.2 Wiki内容审计
- `wiki/` 目录1497个文件，可能存在过时或重复内容
- 使用 wiki-lint 技能检查数据完整性

### 5.3 依赖审计
- 检查requirements.txt中是否有未使用的包
- 前端检查package.json中的devDependencies

## 六、预期效果

| 操作 | 空间节省 | 风险等级 |
|------|---------|---------|
| 删除构建产物 | 2 MB | 无 |
| 删除重复skills | 5 MB | 无 |
| 删除worktrees | 5 MB | 无 |
| 删除实验日志 | 20 MB | 低 |
| 优化checkpoints | 300 MB | 中 |
| **总计** | **332 MB** | - |

## 七、执行步骤

1. ✅ 先运行分析工具确认
2. ✅ 执行无风险清理（第一节）
3. ⚠️ Review checkpoint列表
4. ⚠️ 执行checkpoint清理（需确认）
5. 📝 考虑代码重构（长期）

---

**生成时间：** 2026-06-13
**脚本位置：** scripts/code_cleanup.py, scripts/cleanup_project.py
