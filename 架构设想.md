# Roco-格斗小九

基于Karpathy 的AI知识库架构的一款洛克王国世界PVP对战助手



## wiki

/wiki中保存了PVP相关信息，是agent的知识库。





## skills

- ### 基础行为准则

  - 一切数值判断（伤害、速度、天气影响等）**必须调用对应工具**，禁止凭空估计。
  - 对战状态更新后，**必须输出可读摘要并主动寻求用户确认**，用户修正具有最高优先级。
  - 多方案推演时，**至少提供 2 个不同方向的方案**（如攻击、换人、聚能），并标明风险和前提。
  - 所有输出格式严格遵循各技能要求的示例，保持结构化。

  ------

  ## 技能列表

  ### 1. 查询对战相关信息 `/roco-qurry`

  - **触发时机**：主动查询时，或决策前需要补充信息时。

  - **功能**：从知识库 (`/wiki`) 和对战日志中检索相关数据，包括：

    - 己方/对方精灵的种族、属性、技能、可能配置。
    - 属性克制关系表。
    - 历史上类似对局记录（如有存档）。
    - 当前流行度、常见配招概率等动态数据。

  - **输出**：以列表或小贴士形式呈现查询结果，不包含直接的战术建议。

    

  ### 2. 首发精灵推荐 `/roco-suggest-first`

  - **触发时机**：对局开始时，在 `/roco-eval-team` 和 `/roco-model-opponent`（可选）执行后。

  - **前置动作**：

    1. 自动调用 `/roco-eval-team` 评估双方阵容体系。
    2. 如有对手历史，调用 `/roco-model-opponent` 获取风格卡片。
    3. 调用 `/roco-qurry` 获取克制关系等基础信息。

  - **输出格式**：

    text

    ```
    **首发推荐-精灵名称**
    ```

    

    可附加一句简要理由（如：克制对方首发热门、符合我方体系节奏等），但**必须以粗体推荐行为主要输出**。

    

  ### 3. 下一步建议 `/roco-suggest-next`

  - **触发时机**：每回合我方行动前。

  - **前置动作**：必须已执行 `/roco-think --advanced`，基于其推演结果生成建议。

  - **输出格式**（严格三选一）：

    - `1. 使用技能-技能名称`
    - `2. 切换精灵-精灵名称`
    - `3. 聚能`

  - **附加展示**：应附上从 `/roco-think --advanced` 中选定的方案及其简要理由（如风险可控、收益最高等），以便用户理解。例如：

    text

    ```
    推荐：使用技能-暗夜突袭
    （预期伤害 55%~65%，若对方未换人可击杀，风险为对方换钢系）
    ```

    

  ### 4. 多方案推演与风险提示 `/roco-think --advanced`

  - **触发时机**：每回合决策前（由 `/roco-suggest-next` 自动触发，也可单独调用）。

  - **功能**：结合所有相关工具（伤害计算器、速度线、天气效应、联防评估等）生成 2~3 套可行方案。

  - **输出格式**：

    text

    ```
    【方案A】使用技能-暗夜突袭
       前提：当前速度更快，对方未换人
       预期伤害：55%~65%，可能击杀
       风险：若对方换钢系抵抗，则丧失节奏，或被对面技能应对
    【方案B】切换精灵-地裂装甲
       前提：对方极可能使用电系技能
       收益：无伤上场并建立联防优势
       风险：被读换吃岩系技能重创
    【方案C】聚能
       前提：需要守住关键回合，为后续必杀技蓄力
       收益：获得能量，后续可使用技能
       风险：本回合可能白挨一次攻击，或者被对面应对成功
    ```

    

  - **要求**：必须提供至少两个不同方向（攻击/换人/聚能/强化）的方案，且每个方案必须有明确的“前提、预期伤害/收益、风险”。

  ### 5. 保存对局 `/roco-save`

  - **触发时机**：对局结束后（用户指令或自动询问）。
  - **保存内容**：
    - 完整回合日志与状态变更记录。
    - 所有工具调用记录及其计算结果。
    - **量化复盘指标**：
      - 关键回合决策正确率（事后期望伤害与实际效果对比）。
      - 速度控制权回合比例。
      - 联防成功/失败次数。
      - 致命误判记录（伤害计算偏差 >15% 的回合）。
    - **经验提取**：由复盘算法（而非模型自由发挥）自动分析：
      - 重复出现的劣势模式（如“面对钢系过多清钉手损耗过快”）。
      - 针对该对手的成功操作习惯。
      - 阵容本身暴露的联防漏洞。
  - **输出**：将分析报告存入指定文件夹，并更新个人经验库及对手档案。

  ### 6. 更新对战状态 `/roco-update`

  - **触发时机**：

    - 对局开始时：初始化 12 只精灵实体（己方 6，对方 6）及空白对局日志。
    - 回合开始：记录双方场上精灵及初始状态。
    - 行动后：记录双方使用的技能/操作。
    - 回合结束：结算伤害、状态变化、天气场地等，更新所有精灵实体的当前数据。

  - **状态确认**：**每回合结束必须输出可读摘要并等待用户确认**，格式如下：

    text

    ```
    回合3 结束
    我方：火神 (HP:78%, 攻击+1, 烧伤)
    对方：水灵 (HP:32%, 速度-1)
    操作：火神使用烈焰冲撞，水灵使用水炮，天气转为雨天
    ❓ 请确认以上状态是否正确？(是/否/修正)
    ```

    

  - **修正处理**：若用户指出错误，立即用修正值覆盖并记录“用户修正日志”，后续推理一律以修正后状态为准。

  ### 7. 对手建模 `/roco-model-opponent`

  - **触发时机**：
    - 对局前（可选，若有对手历史）。
    - 每回合后（可选，根据对手操作更新模型）。
  - **功能**：
    - 检索知识库中该对手的历史对战存档。
    - 分析对手偏好：激进/保守比例、常见先读习惯、擅长队伍体系等。
    - 生成本局对手风格卡片，如：“偏好强化终点”“习惯性读换”“首发压天气”等。
  - **输出**：结构化对手风格描述，存入状态层供其他技能调用，不直接展示。

  ### 8. 阵容体系评估 `/roco-eval-team`

  - **触发时机**：在 `/roco-suggest-first` 之前自动执行；也可在对局中对方更换精灵后手动触发。
  - **功能**：
    - 识别我方阵容体系（天气队、空间队、强化推队、平衡联防等）。
    - 评估我方联防弱点、速度线结构、清钉/反清钉能力。
    - 基于已暴露精灵和动态流行数据，猜测对方可能体系。
  - **输出**：简短体系总结（如“我方为雨天强攻，缺乏特盾”），并为首发推荐或中局换人提供体系协同权重。

  ------

  ### 工具调用规范

  - Agent 在执行 `/roco-think --advanced`、`/roco-qurry` 等需要数值支持的技能时，必须主动调用工具列表中的函数，调用顺序建议：
    1. 速度线计算器（判断先手）
    2. 伤害计算器（估算击杀线）
    3. 天气/场地效应计算器（修正伤害与状态）
    4. 配置概率估算器 + 性格分析器（收窄对手配置）
    5. 联防损耗评估器（评估换人代价）
    6. 能量资源追踪器（长期规划）
  - 所有工具返回的数值均以百分比或明确概率呈现，Agent 在转述时不得模糊化。

  



# tools



| 工具名称                | 输入                                                         | 输出                                                         | 用途                                                         |
| :---------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **伤害计算器**          | 攻击方精灵（等级、攻击/特攻种族值、性格修正、天分、当前攻击数值、属性） 攻击技能（威力、属性、是否本系） 被攻击方精灵（防御/特防种族值、性格修正、天分、当前防御数值、属性） 双方增益/减益（攻防等级、反射盾/光之壁等） 当前天气/场地 | 可能伤害范围（最小～最大）， 以被攻击方最大HP的百分比显示    | 精确判断击杀线， 辅助先读、技能选择与换人决策                |
| **性格分析器**          | 攻击方精灵信息（同上） 攻击技能 被攻击方精灵已知信息（种族、属性） 实际造成的伤害值 | 被攻击精灵可能的性格组合（如“+物攻-速度”）及其概率评估       | 反向推断对手精灵性格， 为速度线计算和伤害估算提供依据，提前识别威胁 |
| **速度线计算器**        | 两只精灵（等级、性格、天分、速度种族值、当前速度能力值） 可能携带的技能先制度（+1等） | 明确速度高低判定、谁先出手、是否有先制可能、被超车的概率     | 决定换人时机、技能选择及是否拼速度                           |
| **天气/场地效应计算器** | 当前天气/场地类型 场上精灵属性 技能附加效果（如“晴天时威力×1.5”） | 伤害修正系数、每回合扣血/回复量、异常状态（睡眠、冰冻等）触发概率变化 | 评估天气队/场地队收益，判断是否继续维持或切换                |
| **能量追踪器**          | 我方精灵当前能量， 对手可能消耗情况（如偷能量特性）          | 各技能剩余使用次数、预计枯竭回合、资源持久度评分             | 规划中后期持久战策略，决定是否保留关键技能                   |
| **配置概率估算器**      | 对手精灵种类 当前分段流行数据（来自动态知识库） 已观察到的对手信息（已使用技能、道具触发情况） | 该精灵常见配招概率分布、常见道具/性格概率                    | 辅助性格分析器缩小猜测范围，同时预测对方可能携带的冷门招式   |
| **联防损耗评估器**      | 我方场下所有精灵及其当前血量/状态 对手场下可能精灵（依据阵容推断） 模拟操作序列（如“我方换人A→对方攻击技能B”） | 换人方承受伤害期望及状态变化、是否形成联防闭环（无伤上场）   | 阵容层面的得失判断，提供最优换人路径，避免被迫联防被突破     |



## 整体架构数据流

### [对局开始]

→ /roco-update 初始化12只精灵
→ /roco-eval-team 阵容评估 + /roco-model-opponent 对手建模（若有历史）
→ /roco-qurry 查询克制关系、对战记录、流行配置概率
→ /roco-suggest-first 综合以上所有信息输出首发推荐

### [每回合]

→ /roco-update 同步回合开始状态，展示确认板
→ /roco-think --advanced 调用全部工具进行计算
→ /roco-suggest-next 提供多方案推荐与风险
→ 用户行动后 /roco-update 回合结束更新，并记录工具计算结果

### [对局结束]

→ /roco-save 保存完整日志、工具调用记录、复盘维度分析
→ 提取经验存入个人经验库与对手档案