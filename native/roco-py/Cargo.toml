[package]
name = "roco-py"
version.workspace = true
edition.workspace = true

[lib]
name = "roco_engine"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
numpy = "0.24"
serde_json = "1"
roco-core = { path = "../roco-core" }
